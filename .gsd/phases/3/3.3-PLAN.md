---
phase: 3
plan: 3
wave: 3
depends_on: [1, 2]
files_modified: [src/services/SerialService.ts, src/components/FrequencyControl.tsx, src/components/ExperimentToolbar.tsx]
autonomous: true
must_haves:
  truths:
    - "SerialService exposes a sendCommand(cmd) method"
    - "Changing frequency sends SET_FREQ:<val>"
    - "Toolbar buttons send START, STOP, PAUSE commands"
  artifacts:
    - "src/components/FrequencyControl.tsx calls SerialService"
---

# Plan 3.3: Frequency & Command Transmission

<objective>
To implement the transmission of control commands (`SET_FREQ`, `START`, `PAUSE`, `STOP`) from the user interface down to the `SerialService`. 
Purpose: Send actual hardware control strings so the Arduino can respond.
Output: Integrated `SerialService.sendCommand` logic connected to UI callbacks.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- src/services/SerialService.ts
- src/components/FrequencyControl.tsx
- src/components/ExperimentToolbar.tsx
</context>

<tasks>
<task type="auto">
  <name>Add sendCommand to SerialService</name>
  <files>src/services/SerialService.ts</files>
  <action>
    Add a method `sendCommand(command: string): boolean` to `SerialService`.
    If the status is not 'connected', it should return false or log a warning.
    If connected, it should `console.log(\`[Serial TX] \${command}\`)` (simulating the write over USB OTG).
  </action>
  <verify>Syntax is valid</verify>
  <done>Service can simulate transmission</done>
</task>

<task type="auto">
  <name>Link FrequencyControl to SerialService</name>
  <files>src/components/FrequencyControl.tsx</files>
  <action>
    Import `SerialService`.
    In `handleSetFrequency`, replace the dummy Alert with `SerialService.sendCommand(\`SET_FREQ:\${frequency}\`)`.
  </action>
  <verify>Syntax is valid</verify>
  <done>Set Frequency button transmits correctly</done>
</task>

<task type="auto">
  <name>Link Toolbar to SerialService</name>
  <files>src/components/ExperimentToolbar.tsx</files>
  <action>
    Import `SerialService`.
    In `toggleExperiment`, depending on `isRunning`, send `START` or `PAUSE`.
    In the Stop button `onPress`, send `STOP` and reset `isRunning` to false.
  </action>
  <verify>Syntax is valid</verify>
  <done>Toolbar buttons transmit state commands</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Clicking "Set Frequency" logs `[Serial TX] SET_FREQ:...` to console if connected.
- [ ] Clicking Start/Pause/Stop logs the corresponding commands contextually.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
