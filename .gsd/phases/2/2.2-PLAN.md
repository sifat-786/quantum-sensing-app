---
phase: 2
plan: 2
wave: 2
depends_on: [1]
files_modified: [src/components/FrequencyKnob.tsx, src/components/FrequencyControl.tsx]
autonomous: true
must_haves:
  truths:
    - "Frequency controller uses a circular dial interaction"
    - "Preset buttons are available for quick selection"
  artifacts:
    - "src/components/FrequencyKnob.tsx exists"
---

# Plan 2.2: Interactive Frequency Controller

<objective>
Replace the basic slider with an interactive circular dial/knob. Add preset buttons (5kHz, 10kHz, 25kHz, 40kHz) and real-time animated values.
Purpose: Match the professional tactile feel of an actual lab instrument.
Output: A new knob component integrated into the FrequencyControl.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- src/components/FrequencyControl.tsx
</context>

<tasks>
<task type="auto">
  <name>Install Gesture Dependencies</name>
  <files>package.json</files>
  <action>
    Run `npx expo install react-native-gesture-handler` for handling the circular drag gestures on the knob.
  </action>
  <verify>npm list react-native-gesture-handler</verify>
  <done>Dependency installed</done>
</task>

<task type="auto">
  <name>Create Circular Knob</name>
  <files>src/components/FrequencyKnob.tsx</files>
  <action>
    Create a custom `FrequencyKnob` component using `react-native-gesture-handler` and `react-native-reanimated`. It should allow circular dragging to change internal value (2 Hz - 80 kHz) and display it in the center. Use the neon green/electric blue theming.
  </action>
  <verify>Syntax is valid</verify>
  <done>Knob component exports an interactive dial</done>
</task>

<task type="auto">
  <name>Integrate Presets and Knob</name>
  <files>src/components/FrequencyControl.tsx</files>
  <action>
    Replace the basic Slider in `FrequencyControl` with the new `FrequencyKnob`. Add a row of preset `TouchableOpacity` buttons (5kHz, 10kHz, 25kHz, 40kHz). Tapping a preset should smoothly animate the knob to that value.
  </action>
  <verify>Syntax is valid</verify>
  <done>Frequency control panel behaves as a scientific instrument input</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Knob handles drag gestures
- [ ] Preset buttons update frequency state
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
