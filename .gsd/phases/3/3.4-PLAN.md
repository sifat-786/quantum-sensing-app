---
phase: 3
plan: 4
wave: 4
depends_on: [2, 3]
files_modified: [src/screens/DashboardScreen.tsx, src/components/ExperimentToolbar.tsx, src/components/RealTimeChart.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Only one graph (Capacitance vs Time) is displayed"
    - "Experiment Toolbar is extremely compact with icons"
  artifacts:
    - "DashboardScreen has only one `RealTimeChart` component"
---

# Plan 3.4: Consolidated Graph & Compact Toolbar

<objective>
Remove clutter by dropping multiple graphs. Implement a single Capacitance vs Time graph acting as the primary live visualizer. Simultaneously, compress the `ExperimentToolbar` into a smaller, sleeker, floating or edge-attached icon bar so it doesn't take visually significant space.
Purpose: Finalize the three-tier minimalist structure: Top (Status) -> Middle (Control/Metrics) -> Bottom (Single Graph + Minimal Toolbar).
Output: Updated `DashboardScreen` and `ExperimentToolbar`.
</objective>

<context>
Load for context:
- src/screens/DashboardScreen.tsx
- src/components/ExperimentToolbar.tsx
- src/components/RealTimeChart.tsx
</context>

<tasks>
<task type="auto">
  <name>Consolidate to Single Graph</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Remove the `RealTimeChart` instances for "Res Freq vs Response" and "Impedance vs Time".
    Keep the "Capacitance vs Time" chart.
    Optionally, tweak `RealTimeChart` properties if needed to make that single chart wider or slightly taller.
  </action>
  <verify>Syntax is valid</verify>
  <done>Only Capacitance vs Time is graphed</done>
</task>

<task type="auto">
  <name>Minimalist Experiment Toolbar</name>
  <files>src/components/ExperimentToolbar.tsx</files>
  <action>
    Redesign the toolbar layout out of its large bulky card styling. Use a sleek pill-shaped floating background (`borderRadius: 50`) aligned at the very bottom edge. Reduce text labels beneath icons or eliminate them completely, relying purely on sleek `MaterialCommunityIcons` with Tooltips (if available) or intuitive design.
    Use Cyan for the primary play action and stark red/white for stop.
  </action>
  <verify>Syntax is valid</verify>
  <done>Toolbar is a small, compact row of icons</done>
</task>

<task type="auto">
  <name>Final Layout Pass</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Ensure there's no unused empty space from removed graphs. Adjust margins so the remaining components (Status, Dial, Sensor Row, Single Graph) fill the screen nicely without scrolling excessively if possible, establishing a solid "instrument" feel.
  </action>
  <verify>Syntax is valid</verify>
  <done>Dashboard fits well on modern device ratios</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Only one graph (Capacitance) is on the screen.
- [ ] The experiment toolbar uses a floating compact style.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
