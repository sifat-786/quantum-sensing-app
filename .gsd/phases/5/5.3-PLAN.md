---
phase: 5
plan: 3
wave: 3
depends_on: [1, 2]
files_modified: [src/screens/DashboardScreen.tsx, src/components/RealTimeChart.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Capacitance chart plots historical telemetry data points over time"
    - "Chart auto-scrolls seamlessly"
  artifacts:
    - "RealTimeChart consumes telemetry history"
---

# Plan 5.3: Real-Time Capacitance Charting

<objective>
Feed the live telemetry stream into the "Capacitance VS Time" chart component. Instead of static mock arrays, push the new `telemetry.cap` value into a shifting history array that the chart renders.
Purpose: Achieve beautiful live real-time graph rendering.
Output: Updated `DashboardScreen` managing graph history state and injecting it into `RealTimeChart`.
</objective>

<context>
Load for context:
- src/screens/DashboardScreen.tsx
- src/components/RealTimeChart.tsx
</context>

<tasks>
<task type="auto">
  <name>Implement Chart History Buffer</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Create a constant `MAX_DATA_POINTS = 20`.
    Create a state `capHistory` initialized with an array of 20 zeros (or recent buffer).
    When new `telemetry` data arrives, push the `telemetry.cap` into `capHistory` and slice the array to enforce the `MAX` length, generating a scrolling effect.
    Pass `capHistory` down to the `<RealTimeChart>` data prop.
  </action>
  <verify>Syntax is valid</verify>
  <done>Chart automatically scrolls as new data comes in on the loop</done>
</task>

<task type="auto">
  <name>Optimize Chart Rendering</name>
  <files>src/components/RealTimeChart.tsx</files>
  <action>
    Ensure `react-native-chart-kit` or the internal SVG properly consumes an array and avoids hard flickering by memoizing or styling correctly. 
    Ensure the `withDots={false}` prop is active if the array is dense.
  </action>
  <verify>Syntax is valid</verify>
  <done>Performance is acceptable during interval loops</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] The central graph plots a live, moving line mimicking a heartbeat/sensor readout stream.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
