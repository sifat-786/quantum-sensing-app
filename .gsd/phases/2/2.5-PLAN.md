---
phase: 2
plan: 5
wave: 5
depends_on: [4]
files_modified: [src/components/ExperimentToolbar.tsx, src/screens/DashboardScreen.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "A bottom toolbar exists with experiment control buttons"
  artifacts:
    - "src/components/ExperimentToolbar.tsx exists"
---

# Plan 2.5: Experiment Controls Toolbar

<objective>
Implement a persistent bottom toolbar providing core loop controls: Start Experiment, Pause, Stop, and Export Data.
Purpose: To give the user central command over the overall sensing and data tracking lifecycle.
Output: A new bottom bar pinned to the bottom of the Dashboard or Screen.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- src/screens/DashboardScreen.tsx
</context>

<tasks>
<task type="auto">
  <name>Create Experiment Toolbar</name>
  <files>src/components/ExperimentToolbar.tsx</files>
  <action>
    Create a component containing four icon-text horizontal or grid buttons using `@expo/vector-icons` (Play for Start, Pause, Stop square, Download for Export). Style the toolbar with a dark glassy/opaque background (`colors.cardBackground` with transparency) to pin at the bottom of the UI.
  </action>
  <verify>Syntax is valid</verify>
  <done>Toolbar exposes 4 main action buttons</done>
</task>

<task type="auto">
  <name>Pin Toolbar to Dashboard</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Import and render `ExperimentToolbar` at the very bottom of the `DashboardScreen`, outside of the main ScrollView (or as a pinned footer), ensuring it's always visible above the graphs.
  </action>
  <verify>Syntax is valid</verify>
  <done>Toolbar is persistently visible at the bottom of the dashboard area</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Toolbar renders Start, Pause, Stop, and Export buttons
- [ ] Toolbar maintains position at the bottom of the viewport
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
