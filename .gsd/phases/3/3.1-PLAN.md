---
phase: 3
plan: 1
wave: 1
depends_on: []
files_modified: [src/theme/theme.ts, src/components/StatusBanner.tsx, src/screens/DashboardScreen.tsx]
autonomous: true
must_haves:
  truths:
    - "Theme utilizes deep blacks, cyan, and clean rounded styles"
    - "Status indicator is minimal and anchors the top"
  artifacts:
    - "src/theme/theme.ts exports cyan as primary"
---

# Plan 3.1: Tech Minimalist Theme & Global Adjustments

<objective>
To refine the overarching visual direction of the app to feel like a modern, minimalist tech dashboard (e.g., Apple Health, Tesla UI).
Purpose: Establish the base colors, shadows, and simplified top-level structure.
Output: An updated `theme.ts` setting Cyan/Deep Black aesthetics, and a simplified `StatusBanner` integrated cleanly at the top of the screen.
</objective>

<context>
Load for context:
- src/theme/theme.ts
- src/components/StatusBanner.tsx
- src/screens/DashboardScreen.tsx
</context>

<tasks>
<task type="auto">
  <name>Update Theme Palette & Styles</name>
  <files>src/theme/theme.ts</files>
  <action>
    Change `colors.background` to deeply black (`#000000` or `#050505`).
    Change `colors.primary` to Cyan (`#00FFFF` or similar).
    Adjust `colors.cardBackground` to be slightly raised from deep black (e.g. `#111111`).
    Simplify glows by reducing blur radius and opacity so they feel "soft" rather than "neon".
    Increase `borderRadius` in `globalStyles.card` to `20` or `24` for a softer, premium look.
  </action>
  <verify>Syntax is valid</verify>
  <done>Theme exposes refined modern style variables</done>
</task>

<task type="auto">
  <name>Refine StatusBanner</name>
  <files>src/components/StatusBanner.tsx</files>
  <action>
    Simplify the StatusBanner. Make the border transparent or completely remove the box styling, presenting it as floating minimal text + indicator at the top of the dashboard. Reduce padding to fit nicely within standard safe areas.
  </action>
  <verify>Syntax is valid</verify>
  <done>StatusBanner is simple and unobtrusive</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] UI background looks deep black/dark gray.
- [ ] Status banner has lost its bulky card wrapper.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
