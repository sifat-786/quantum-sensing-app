---
phase: 5
plan: 1
wave: 1
depends_on: []
files_modified: [src/services/SerialService.ts]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "SerialService outputs realistic mocked formatted telemetry"
    - "Data is broadcast to subscribers at regular intervals"
  artifacts:
    - "SerialService telemetry loop is active"
---

# Plan 5.1: Serial Telemetry Engine

<objective>
Upgrade the `SerialService` mock to not only maintain a connection state, but to actively broadcast formatted mock hardware telemetry (`FREQ`, `CAP`, `RES`, `IMP`) at regular intervals when connected.
Purpose: Create a reliable data stream mimicking the Arduino output array.
Output: Updated `SerialService` containing a running mock data loop and a data subscription architecture.
</objective>

<context>
Load for context:
- src/services/SerialService.ts
</context>

<tasks>
<task type="auto">
  <name>Implement Telemetry Loop</name>
  <files>src/services/SerialService.ts</files>
  <action>
    Add a new type `TelemetryData` with fields for `cap` (number), `res` (number), `imp` (number), and `freq` (number).
    Add a new `onData` listener system mechanism specifically for high-frequency telemetry.
    When `status` becomes `connected`, start a `setInterval` loop (~100ms) that generates slightly randomized but realistic physical values based on the currently set frequency.
    Broadcast this raw JSON/object payload to all `onData` subscribers.
    When `status` becomes `disconnected` or parsing hits a "STOP", clear the interval.
  </action>
  <verify>Syntax is valid and Interval runs cleanly</verify>
  <done>Service actively broadcasts data</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Console logs a steady stream of data objects when "connected".
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
