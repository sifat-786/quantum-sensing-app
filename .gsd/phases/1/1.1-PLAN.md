---
phase: 1
plan: 1
wave: 1
depends_on: []
files_modified: [package.json, App.tsx, src/theme/theme.ts, src/types/index.ts]
autonomous: true
must_haves:
  truths:
    - "React Native Expo project has required UI structure and dependencies"
    - "Dark theme with Electric Blue and Neon Green is applied via a central theme file"
  artifacts:
    - "src/theme/theme.ts exists"
    - "App.tsx is the entrypoint"
---

# Plan 1.1: Project Setup and Theming

<objective>
Initialize the core React Native project structure using Expo, install essential UI packages, and apply the scientific dark theme.
Purpose: Establishes the foundation for all UI components.
Output: Themed entrypoint and dependency setup.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Initialize Expo Project & Dependencies</name>
  <files>package.json, app.json, App.tsx</files>
  <action>
    Initialize a new Expo project in the current directory (if not already done). Add required UI dependencies (e.g., `@expo-google-fonts/inter`, `expo-font`). AVOID adding USB/Serial packages yet; just UI setup. Ensure `App.tsx` exists as the entry point.
  </action>
  <verify>npx expo --version and project files exist</verify>
  <done>Base Expo project is ready</done>
</task>

<task type="auto">
  <name>Create App Theme</name>
  <files>src/theme/theme.ts</files>
  <action>
    Create a centralized theme object providing a dark background (`#121212`), Electric Blue primary color (`#00F0FF` approx), Neon Green accent (`#39FF14`), and white text. Expose text styles and colors for easy consumption by components.
  </action>
  <verify>Syntax is valid</verify>
  <done>src/theme/theme.ts exports theme constants</done>
</task>

<task type="auto">
  <name>Initialize App Entrypoint</name>
  <files>App.tsx</files>
  <action>
    Setup `App.tsx` to use the theme and load fonts. Clear default boilerplates. Show an empty dark View with a placeholder "AQC Sensor Dashboard" text styled with the primary colors.
  </action>
  <verify>TypeScript compiles (if using TS) or syntax is valid</verify>
  <done>Clean entrypoint with custom theme applied</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Theme applies dark mode, blue primary, green accent in App.tsx
- [ ] Expo project compiles/starts
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
