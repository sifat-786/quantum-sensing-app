---
phase: 4
plan: 2
wave: 2
depends_on: [1]
files_modified: [src/screens/DashboardScreen.tsx, src/components/StatusBanner.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Dashboard status indicators react to SerialService"
  artifacts:
    - "DashboardScreen manages global connection state"
---

# Plan 4.2: Dashboard Connection Integration

<objective>
Wire up the UI to the new `SerialService`. The `StatusBanner` should reflect the actual simulated connection state, and the Dashboard should attempt to connect when mounted (or via a manual trigger).
Purpose: Bring the mocked hardware state into the visual interface.
Output: Updated `DashboardScreen` and `StatusBanner` logic.
</objective>

<context>
Load for context:
- src/screens/DashboardScreen.tsx
- src/components/StatusBanner.tsx
- src/services/SerialService.ts
</context>

<tasks>
<task type="auto">
  <name>Connect Dashboard State</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Import `serialService`.
    Add a `useEffect` hook that subscribes to `serialService.addListener`.
    Maintain a local `connectionStatus` state initialized to `serialService.getStatus()`.
    On mount, automatically call `serialService.connect()`.
    Pass the `connectionStatus` to the `StatusBanner`.
  </action>
  <verify>Syntax is valid</verify>
  <done>Dashboard correctly monitors raw connection state</done>
</task>

<task type="auto">
  <name>Refine StatusBanner Props</name>
  <files>src/components/StatusBanner.tsx</files>
  <action>
    Ensure `StatusBanner` accepts the live `status` prop and animates appropriately based on the state transitions (already built in Phase 2/3, just ensure props map correctly).
  </action>
  <verify>Syntax is valid</verify>
  <done>Banner reflects live data</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Loading the dashboard triggers a "Connecting..." animation followed by "Connected".
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
