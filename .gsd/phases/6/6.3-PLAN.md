---
phase: 6
plan: 3
wave: 3
depends_on: [2]
files_modified: [src/components/FrequencyControl.tsx, src/services/SerialService.ts]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Auto Sweep command triggers stepping frequency logic"
  artifacts:
    - "Sweep Toggle exists in UI"
---

# Plan 6.3: Auto Frequency Sweep

<objective>
Implement the "Optional" Feature 8 from the SPEC: an automated sweep sequence that steps the frequency from a starting bound to an ending bound automatically, logging the responses at each step.
Purpose: Alleviate manual dialing for broad spectrum quantum sampling.
Output: A new "Sweep" toggle and sweep stepping logic in the service layer.
</objective>

<context>
Load for context:
- src/components/FrequencyControl.tsx
- src/services/SerialService.ts
</context>

<tasks>
<task type="auto">
  <name>Implement Service Layer Sweeping</name>
  <files>src/services/SerialService.ts</files>
  <action>
    Introduce a `isSweeping` boolean.
    Add handling for a UART command `SWEEP_START`.
    When `isSweeping` is active, alter the `startTelemetryLoop` to increment `currentFreq` by 100Hz on every tick.
    If `currentFreq > 60000`, wrap around or stop the sweep.
    Ensure `telemetry.freq` accurately broadcasts this changing value so the UI dial turns visually on its own.
  </action>
  <verify>Syntax is valid</verify>
  <done>Service can simulate an automated hardware sweep</done>
</task>

<task type="auto">
  <name>Add Sweep Toggle</name>
  <files>src/components/FrequencyControl.tsx</files>
  <action>
    Add a subtle "Auto Sweep" `<TouchableOpacity>` pill next to or below the manual Presets.
    When tapped, dispatch `serialService.sendCommand('SWEEP_START')` or `SWEEP_STOP`.
  </action>
  <verify>Syntax is valid</verify>
  <done>UI allows users to trigger the sweep</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Tapping Auto Sweep causes the telemetry frequency to ramp up continuously without user finger input on the dial.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
