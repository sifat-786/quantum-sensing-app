---
phase: 4
plan: 3
wave: 3
depends_on: [1, 2]
files_modified: [src/components/FrequencyControl.tsx, src/components/ExperimentToolbar.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Changing frequency fires a SET_FREQ serial command"
    - "Play/stop buttons fire START/STOP serial commands"
  artifacts:
    - "UI interactions map directly to Serial commands"
---

# Plan 4.3: Command Transmission

<objective>
Link the interactive user controls (Frequency Dial, Presets, and Toolbar buttons) to the `SerialService.sendCommand()` method, effectively bridging user intent to the simulated hardware.
Purpose: Complete the outbound data flow from the app to the device.
Output: Updated `FrequencyControl` and `ExperimentToolbar` components with live command hooks.
</objective>

<context>
Load for context:
- src/components/FrequencyControl.tsx
- src/components/ExperimentToolbar.tsx
- src/services/SerialService.ts
</context>

<tasks>
<task type="auto">
  <name>Bind Frequency Commands</name>
  <files>src/components/FrequencyControl.tsx</files>
  <action>
    Import `serialService`.
    In `handleKnobChange` and `setPreset`, after updating the local state, invoke `serialService.sendCommand(\`SET_FREQ:\${val}\`)`.
  </action>
  <verify>Syntax is valid</verify>
  <done>Frequency changes send UART string formats</done>
</task>

<task type="auto">
  <name>Bind Toolbar Commands</name>
  <files>src/components/ExperimentToolbar.tsx</files>
  <action>
    Import `serialService`.
    In `toggleExperiment`, invoke `serialService.sendCommand(isRunning ? 'PAUSE' : 'START')`.
    Attach an `onPress` to the Stop button that invokes `serialService.sendCommand('STOP')`.
  </action>
  <verify>Syntax is valid</verify>
  <done>Toolbar actions send hardware commands</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Rotating the dial outputs `SET_FREQ:X` to the console.
- [ ] Pressing Play outputs `START` to the console.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
