---
phase: 3
plan: 2
wave: 2
depends_on: [1]
files_modified: [src/screens/DashboardScreen.tsx, src/components/StatusBanner.tsx]
autonomous: true
must_haves:
  truths:
    - "Dashboard status connects to the SerialService state"
    - "User can trigger connect/disconnect from the UI"
  artifacts:
    - "DashboardScreen imports SerialService"
---

# Plan 3.2: Dashboard Connection Integration

<objective>
Integrate the newly created `SerialService` with the `StatusBanner` and `ExperimentToolbar` so that the UI reflects the connection status accurately, and so the user can connect/disconnect from the USB device.
Purpose: Allow user control and visibility of the hardware link.
Output: Dynamic hook binding to `SerialService` in `DashboardScreen`.
</objective>

<context>
Load for context:
- src/screens/DashboardScreen.tsx
- src/services/SerialService.ts
</context>

<tasks>
<task type="auto">
  <name>Make StatusBanner Touchable</name>
  <files>src/components/StatusBanner.tsx</files>
  <action>
    Wrap the `StatusBanner` in a `TouchableOpacity` and accept an `onPress` callback. This allows the user to tap the banner to connect or disconnect manually for prototype testing.
  </action>
  <verify>Syntax is valid</verify>
  <done>StatusBanner fires onPress events</done>
</task>

<task type="auto">
  <name>Bind Service to Dashboard Status</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Inside `DashboardScreen`, set up useEffect to subscribe to `SerialService.onStatusChange`.
    Store the connection status in React state.
    Pass this state to `StatusBanner`.
    When the `StatusBanner` is tapped, call `SerialService.connect()` if disconnected, or `disconnect()` if connected.
  </action>
  <verify>Syntax is valid</verify>
  <done>UI correctly displays backend connection state</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Tapping the status banner visually transitions UI from Disconnected -> Connecting -> Connected.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
