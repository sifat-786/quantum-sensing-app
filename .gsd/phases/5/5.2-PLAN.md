---
phase: 5
plan: 2
wave: 2
depends_on: [1]
files_modified: [src/screens/DashboardScreen.tsx, src/components/SensorCard.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Dashboard metrics animate based on live incoming SerialService data"
  artifacts:
    - "SensorCards react to telemetry data"
---

# Plan 5.2: Live Sensor Metrics

<objective>
Connect the static Dashboard Sensor Cards (Capacitance, Resonant Frequency, Impedance) to the `SerialService` telemetry stream so they vividly update with live numbers.
Purpose: Visualize the incoming raw data on the primary dashboard interface.
Output: Updated `DashboardScreen` pushing state bindings to `SensorCard` props.
</objective>

<context>
Load for context:
- src/screens/DashboardScreen.tsx
- src/components/SensorCard.tsx
- src/services/SerialService.ts
</context>

<tasks>
<task type="auto">
  <name>Bind Telemetry to Dashboard State</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Add a state `telemetry` (type `TelemetryData`) in the `DashboardScreen`.
    Subscribe to `serialService.addDataListener` in the `useEffect` block.
    Pipe the live `telemetry.cap`, `telemetry.res`, and `telemetry.imp` values directly into the corresponding `<SensorCard>` component props.
  </action>
  <verify>Syntax is valid</verify>
  <done>Sensor Cards flash with live incoming data</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] The hardware metric numbers animate wildly when connected, proving the binding works.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
