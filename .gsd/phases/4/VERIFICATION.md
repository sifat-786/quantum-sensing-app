## Phase 4 Verification

### Must-Haves
- [x] `SerialService.ts` exists and exposes singleton - VERIFIED
- [x] Service supports mock behavior for web simulation (`connecting`, `connected`) - VERIFIED
- [x] The Dashboard connection state reacts and triggers the `StatusBanner` indicator - VERIFIED
- [x] Changing frequency output logs `SET_FREQ` UART strings via the service - VERIFIED
- [x] Toggling or stopping the experiment outputs `START`, `PAUSE`, and `STOP` UART strings via the service - VERIFIED

### Verdict: PASS
