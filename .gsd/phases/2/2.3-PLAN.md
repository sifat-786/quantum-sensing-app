---
phase: 2
plan: 3
wave: 3
depends_on: [1]
files_modified: [src/components/SensorCard.tsx, src/screens/DashboardScreen.tsx]
autonomous: true
must_haves:
  truths:
    - "Sensor cards display the required metrics with label and animated values"
    - "Cards flash subtly or glow when data updates"
  artifacts:
    - "src/components/SensorCard.tsx exists"
---

# Plan 2.3: Animated Live Sensor Cards

<objective>
Replace the placeholder text for "Live Sensor Data" with three individual animated data cards (Quantum Capacitance, Resonant Frequency, Impedance).
Purpose: To display numerical data dynamically with visual feedback on change, simulating a responsive hardware interface.
Output: A reusable `SensorCard` component integrated into the dashboard.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- src/screens/DashboardScreen.tsx
</context>

<tasks>
<task type="auto">
  <name>Create Sensor Card Component</name>
  <files>src/components/SensorCard.tsx</files>
  <action>
    Create a `SensorCard` component that accepts `title`, `value`, `unit`, and `color`. Use `react-native-reanimated` to implement a subtle color glow effect that triggers whenever the `value` prop changes. Add a purely decorative, static SVG "sparkline" using `react-native-svg` to simulate historical data.
  </action>
  <verify>Syntax is valid</verify>
  <done>SensorCard supports animated value updates and a decorative sparkline</done>
</task>

<task type="auto">
  <name>Integrate Sensor Cards into Dashboard</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Replace the "Live Sensor Data" placeholder card with a horizontal or grid layout containing three `SensorCard` instances: "Quantum Capacitance" (pF), "Resonant Frequency" (kHz), and "Impedance" (kΩ). Use dummy state values for now.
  </action>
  <verify>Syntax is valid</verify>
  <done>Dashboard renders the three distinct sensor cards</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Sensor cards display in the dashboard
- [ ] Changing a card's value programmatically triggers an animation (verified visually or logically)
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
