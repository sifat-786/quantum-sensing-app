---
phase: 4
plan: 1
wave: 1
depends_on: []
files_modified: [src/services/SerialService.ts]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "SerialService exists as a singleton"
    - "It supports mock behavior for web development (simulation)"
  artifacts:
    - "src/services/SerialService.ts is created"
---

# Plan 4.1: Core Serial Service Architecture

<objective>
Establish the foundational data layer for USB OTG serial communication. Since the app is frequently tested on the web simulator, build a singleton `SerialService` that successfully mimics connection states and command transmission before native hardware libraries are introduced.
Purpose: Create a decoupled service layer that the UI can blindly trust for hardware operations.
Output: A new `SerialService.ts` file handling state, mock connections, and event listeners.
</objective>

<context>
Load for context:
- (None, this is a new service)
</context>

<tasks>
<task type="auto">
  <name>Create SerialService</name>
  <files>src/services/SerialService.ts</files>
  <action>
    Create a class `SerialService`.
    Include properties for `status` ('disconnected', 'connecting', 'connected').
    Implement `connect()` which sets status to 'connecting', waits 1500ms using `setTimeout`, then sets to 'connected' and fires listeners.
    Implement `disconnect()`.
    Implement `sendCommand(cmd: string)` which logs the command to the console.
    Implement a simple subscription model (`addListener`, `removeListener`) so React components can react to status changes.
    Export a singleton instance `export const serialService = new SerialService();`.
  </action>
  <verify>Syntax is valid</verify>
  <done>SerialService is ready for consumption</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] `src/services/SerialService.ts` exports a singleton instance.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
