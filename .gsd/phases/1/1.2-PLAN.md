---
phase: 1
plan: 2
wave: 2
depends_on: [1]
files_modified: [src/screens/DashboardScreen.tsx, src/components/FrequencyControl.tsx, src/components/StatusBanner.tsx, App.tsx]
autonomous: true
must_haves:
  truths:
    - "Dashboard follows the top-to-bottom layout described in SPEC"
    - "Frequency controller prevents exceeding 80kHz visually"
  artifacts:
    - "src/screens/DashboardScreen.tsx exists"
---

# Plan 1.2: Dashboard UI Foundation

<objective>
Construct the main Dashboard layout: Status Banner, Frequency Control, and placeholders for Data/Graphs.
Purpose: Visually satisfy the primary UI requirement.
Output: The main screen interface.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- App.tsx
- src/theme/theme.ts
</context>

<tasks>
<task type="auto">
  <name>Create Status Banner</name>
  <files>src/components/StatusBanner.tsx</files>
  <action>
    Create a React Native component (`View` and `Text`) to display connection status (e.g., "Device Disconnected" returning red background, "Device Connected" returning green). Use dummy state props for now.
  </action>
  <verify>Syntax is valid</verify>
  <done>Component created</done>
</task>

<task type="auto">
  <name>Create Frequency Control</name>
  <files>src/components/FrequencyControl.tsx, package.json</files>
  <action>
    Install `@react-native-community/slider`. Implement a view containing a Slider (2Hz to 80,000Hz), a `TextInput` for numeric input, and a "Set Frequency" `TouchableOpacity` button. Add local state logic to cap numeric input at 80,000 and show a visual warning (e.g., red text or standard `Alert`) if exceeded. Use dummy callback for now.
  </action>
  <verify>Package installed and syntax is valid</verify>
  <done>Frequency control UI with slider and input is ready</done>
</task>

<task type="auto">
  <name>Assemble Dashboard</name>
  <files>src/screens/DashboardScreen.tsx, App.tsx</files>
  <action>
    Combine StatusBanner and FrequencyControl into the top sections of a new `DashboardScreen` component. Add empty `View` placeholders for "Live Sensor Data" and "Real-Time Graphs". Render `DashboardScreen` inside `App.tsx`.
  </action>
  <verify>Syntax is valid</verify>
  <done>Dashboard is the main view and displays components</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Dashboard shows status
- [ ] Dashboard shows frequency control capable of adjusting 2Hz-80kHz
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
