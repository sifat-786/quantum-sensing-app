---
phase: 2
plan: 1
wave: 1
depends_on: []
files_modified: [src/theme/theme.ts, src/components/StatusBanner.tsx]
autonomous: true
must_haves:
  truths:
    - "Application has scientific instrument styling gradients and glows"
    - "Status banner has pulse/spinner animations and USB icon"
  artifacts:
    - "src/components/StatusBanner.tsx uses animation"
---

# Plan 2.1: Scientific Theming & Device Status Panel

<objective>
Upgrade the central theme to a purely scientific lab aesthetic. Then, upgrade the Status Banner to support disconnected/connecting/connected states with animations and a USB icon.
Purpose: Create the foundational look for a professional instrument and improve connection feedback.
Output: Upgraded theme library and StatusBanner component.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- src/theme/theme.ts
</context>

<tasks>
<task type="auto">
  <name>Install Icon & Animation Dependencies</name>
  <files>package.json</files>
  <action>
    Run `npx expo install @expo/vector-icons react-native-reanimated`. We need standard icons for the USB symbol and reanimated for smooth pulse/spinner transitions.
  </action>
  <verify>npm list @expo/vector-icons</verify>
  <done>Dependencies installed</done>
</task>

<task type="auto">
  <name>Upgrade Theme</name>
  <files>src/theme/theme.ts</files>
  <action>
    Add complex styling helpers such as soft glows (using shadow properties) for the primary and accent colors, and define state colors (disconnected: red/pulse, connecting: yellow, connected: green).
  </action>
  <verify>TypeScript compilation</verify>
  <done>Theme exports glow styles and connection state colors</done>
</task>

<task type="auto">
  <name>Animated Status Banner</name>
  <files>src/components/StatusBanner.tsx</files>
  <action>
    Update StatusBanner to accept a `status` enum ('disconnected', 'connecting', 'connected'). Add a USB icon from `@expo/vector-icons`. Implement a red pulse animation for 'disconnected' and a spinner for 'connecting' using `react-native-reanimated`.
  </action>
  <verify>Syntax is valid</verify>
  <done>Banner handles 3 states with respective animations</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Theme holds scientific glowing styles
- [ ] Status Banner logic implements animations for different connection states
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
