---
phase: 3
plan: 2
wave: 2
depends_on: [1]
files_modified: [src/components/FrequencyKnob.tsx, src/components/FrequencyControl.tsx, src/screens/DashboardScreen.tsx]
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Frequency controller is front and center on the Dashboard"
    - "Dial uses a minimalist, thin-line tech aesthetic"
    - "Numbers are large and prominent"
  artifacts:
    - "src/components/FrequencyKnob.tsx looks like a modern instrument dial"
---

# Plan 3.2: Large Central Frequency Dial

<objective>
Refine the Frequency Knob to be significantly larger, acting as the centerpiece of the dashboard. Instead of a thick glowing border, transition to a sharp, thin-line minimalist tech aesthetic (resembling a futuristic car dashboard dial).
Purpose: Establish the primary user interaction point with high visual impact.
Output: Updated `FrequencyKnob` and `FrequencyControl` components, restructured within the `DashboardScreen`.
</objective>

<context>
Load for context:
- src/components/FrequencyKnob.tsx
- src/components/FrequencyControl.tsx
- src/screens/DashboardScreen.tsx
</context>

<tasks>
<task type="auto">
  <name>Redesign FrequencyKnob</name>
  <files>src/components/FrequencyKnob.tsx</files>
  <action>
    Increase `KNOB_SIZE` to `280` or `300`. Remove the thick `borderWidth` and instead use a thin (`1px` or `2px`) line for the dial track. Increase the text size of the central metric drastically (`fontSize: 48` or `56`). Make the indicator more subtle, perhaps a small dot or thin tick mark. Remove excessive glows and simplify shadows.
  </action>
  <verify>Syntax is valid</verify>
  <done>Knob looks vastly simplified and larger</done>
</task>

<task type="auto">
  <name>Refine FrequencyControl Layout</name>
  <files>src/components/FrequencyControl.tsx</files>
  <action>
    Remove the `globalStyles.card` wrapper to let the dial float natively on the deep black background. Hide the text input entirely to rely purely on the dial and presets. Arrange the preset buttons symmetrically below the knob with a very subtle glass or transparent styling.
  </action>
  <verify>Syntax is valid</verify>
  <done>Control layout relies only on Dial + Presets without boxy enclosures</done>
</task>

<task type="auto">
  <name>Position in Dashboard</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Ensure `FrequencyControl` is the immediate second child after `StatusBanner`, occupying the prime center real estate. Use Flexbox to give it appropriate vertical spacing.
  </action>
  <verify>Syntax is valid</verify>
  <done>Frequency control is the focal point</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] Frequency dial is visually dominant on the screen.
- [ ] Text-input field is gone, replaced entirely by visual dial and presets.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
