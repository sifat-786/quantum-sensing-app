---
phase: 2
plan: 4
wave: 4
depends_on: [3]
files_modified: [package.json, src/components/RealTimeChart.tsx, src/screens/DashboardScreen.tsx]
autonomous: true
must_haves:
  truths:
    - "A charting library is integrated into the application"
    - "Dashboard contains three distinct graphical charts with neon styling"
  artifacts:
    - "src/components/RealTimeChart.tsx exists"
---

# Plan 2.4: Real-Time Graphs Integration

<objective>
Implement actual charts using `react-native-chart-kit` or `victory-native` to replace the "Real-Time Graphs" placeholder text.
Purpose: Visualize the live experiment metrics with a neon, lab-style plotting interface.
Output: Integrated charting components in the dashboard.
</objective>

<context>
Load for context:
- .gsd/SPEC.md
- src/screens/DashboardScreen.tsx
</context>

<tasks>
<task type="auto">
  <name>Install Chart Dependencies</name>
  <files>package.json</files>
  <action>
    Run `npx expo install react-native-chart-kit react-native-svg` (if not already installed). This provides the tools needed for drawing scientific graphs.
  </action>
  <verify>npm list react-native-chart-kit</verify>
  <done>Charting library installed</done>
</task>

<task type="auto">
  <name>Create RealTimeChart Component</name>
  <files>src/components/RealTimeChart.tsx</files>
  <action>
    Create a reusable `RealTimeChart` component that wraps the `LineChart` from `react-native-chart-kit`. Configure it to have transparent chart backgrounds, neon grid lines (using stroke properties and low opacity), and line colors that match the `theme.ts` (electric blue/neon green). Accept dummy dataset arrays for now.
  </action>
  <verify>Syntax is valid</verify>
  <done>RealTimeChart renders an styled data line</done>
</task>

<task type="auto">
  <name>Add Charts to Dashboard</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Replace the "Real-Time Graphs" placeholder with three `RealTimeChart` instances: "Capacitance vs Time", "Frequency vs Response", and "Impedance vs Time", scrolling within a container if necessary.
  </action>
  <verify>Syntax is valid</verify>
  <done>Charts populate the lower half of the Dashboard</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] No placeholder text remains in the chart section
- [ ] Three graphical charts are rendered using transparent backgrounds and neon lines
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
