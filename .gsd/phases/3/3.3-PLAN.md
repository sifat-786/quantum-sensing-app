---
phase: 3
plan: 3
wave: 3
depends_on: [1]
files_modified: [src/components/SensorCard.tsx, src/screens/DashboardScreen.tsx]
autonomous: true
must_haves:
  truths:
    - "Sensor cards maintain large numbers but remove the sparkline graphs"
    - "Cards display concisely below the frequency dial"
  artifacts:
    - "src/components/SensorCard.tsx is simplified"
---

# Plan 3.3: Clean Sensor Dashboard Cards

<objective>
Simplify the `SensorCard` component to align with the minimalist tech dashboard aesthetic. Remove the `react-native-svg` sparkline graphs to reduce visual clutter, focusing entirely on a large, clean metric display with a unit and a subtle trend or status color.
Purpose: Reduce cognitive load and hierarchical clutter on the dashboard.
Output: Refactored `SensorCard` and updated `DashboardScreen` layout for the metrics row.
</objective>

<context>
Load for context:
- src/components/SensorCard.tsx
- src/screens/DashboardScreen.tsx
</context>

<tasks>
<task type="auto">
  <name>Simplify SensorCard</name>
  <files>src/components/SensorCard.tsx</files>
  <action>
    Remove the `Svg` and `Path` components completely.
    Remove the `sparklineContainer` references.
    Increase the font size of the `value` drastically (e.g., `typography.h2` or larger custom style).
    Keep the border glow animation on value change, but make it very subtle.
    Ensure the layout centers or cleanly left-aligns the metric and unit.
  </action>
  <verify>Syntax is valid</verify>
  <done>Sensor cards display only pure metrics and units</done>
</task>

<task type="auto">
  <name>Update Dashboard Row Layout</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <action>
    Ensure the `sensorsRow` aligns cleanly under the new large `FrequencyControl`.
    Remove or reduce the spacing between the cards to make them look like a unified horizontal control panel.
  </action>
  <verify>Syntax is valid</verify>
  <done>Sensor metrics sit neatly in the lower-middle half of the screen</done>
</task>
</tasks>

<verification>
After all tasks, verify:
- [ ] No graphs appear inside the three sensor cards.
- [ ] Sensor cards emphasize large Typography.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
